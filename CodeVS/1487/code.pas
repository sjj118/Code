var
  n,i,x:longint;
  a:array[1..10]of longint;
begin
  readln(n);
  for i:=1 to n do begin
    readln(x);
	x:=0;
	//inc(a[x]);
	//a[x]:=maxlongint;
  end;
  //for i:=1 to 10 do for x:=1 to a[i] do writeln(i);
end.
